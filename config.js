const path = require("path");

module.exports = {
  siteTitle: "Keari Eggers | Front-end developer",
  paths: {
    src: path.resolve(__dirname, "src"),
    dist: path.resolve(__dirname, "dist")
  }
};
